package org.tzi.use.rest.controller;

public class ClassController {
    /*
    Endpoint that are needed prefix /api/model/{id}
    class should only have for attribute and operation (and assoc)
     */
}
