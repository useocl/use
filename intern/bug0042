$Bug: Inconsistent results: asSequence for sets and bags$
$Category: OCL$
$State: Closed$
$BugAuthor: mk$
$Tests: test/t041$

DESCRIPTION:

  use> ?Bag{3,2,1,1}->asSequence()
  -> Sequence{3,1,1,2} : Sequence(Integer)
  use> ?Set{3,2,1,1}->asSequence()
  -> Sequence{1,2,3} : Sequence(Integer)

SOLUTION: 

  (Was already fixed in an earlier version.) 
  Used TreeMap instead of HashMap to store the actual values in BagValue.
