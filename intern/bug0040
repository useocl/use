$Bug: Conversion results (asSequence/asBag) inconsistent$
$Category: OCL$
$State: Closed$
$BugAuthor: mk$
$Tests: test/t041$

DESCRIPTION:
  
  Example:
  ?let s=Set{41,42} in s->asSequence()=s->asBag()->asSequence()
  false : Boolean
  
  ?let s=Set{42,43} in s->asSequence()=s->asBag()->asSequence()
  true : Boolean

SOLUTION: 

  (Was already fixed in an earlier version.) 
  Used TreeMap instead of HashMap to store the actual values in BagValue. 
