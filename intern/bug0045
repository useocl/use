$Bug: OCL standard requires parentheses for allInstances$
$Category: OCL$
$State: Closed$
$BugAuthor: mk$
$Tests: test/t038$

DESCRIPTION:
 
  Person.allInstances() does not work, only Person.allInstances

SOLUTION:

  Parentheses are now allowed (but still optional).
