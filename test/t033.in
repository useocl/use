#Test the errors when creating a new composition link:
# The error of the cyclic Composition
# The error of the shared part
# The error of the duplicated link

!create section, subsection, subsubsection: Part
!insert (section, subsection) into Inclusion
!insert (subsection, subsubsection) into Inclusion

!insert (subsubsection, section) into Inclusion
*The error of the cyclic Composition

!insert (section, subsubsection) into Inclusion
*The error of the shared part (subsubsection)

!insert (section, subsection) into Inclusion
*The error of the duplicated link